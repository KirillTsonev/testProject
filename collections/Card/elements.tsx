import styled from "styled-components";

export const StyledCardContainer = styled(({...props}) => <div {...props} />)`
  display: flex;
  align-items: center;
  background: #eeeeee;
  padding: 10px;
`;
